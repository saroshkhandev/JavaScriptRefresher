// Destructuring

const numbers = [1, 2, 3];

[num1, num2] = numbers;

console.log(num1, num2);

console.log(numbers);

// this destructing method is used to extract the elements from an array to the variables
// or extracting the properties from objects to the variables.