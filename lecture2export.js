const myFunction = (name) => {
    return name + "How are you?";
};

export default myFunction;

// another way of exporting

export const myFunction2 = (name) => {
    return name + "is the best";
};

// you can also export the variables

export const value = 50;

